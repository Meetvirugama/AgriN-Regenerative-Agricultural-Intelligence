-- AgriMesh Migration 021: Add crop_type to regen_plans
-- Depends on: 019_regen_plans.sql

-- [DEAD CODE REMOVED] This migration is a duplicate of 019_regen_plans.sql
-- which already explicitly included the crop_type column upon table creation.
-- The file is preserved as a tombstone so the migration runner skips it gracefully.
